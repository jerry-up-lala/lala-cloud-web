export const USER_URL = '/system/user';

export const USER_LIST_URL = `${USER_URL}/list`;

export const USER_PAGE_URL = `${USER_URL}/page`;

export const USER_INFO_URL = `${USER_URL}/info/`;

export const USER_PASSWORD_URL = `${USER_URL}/password/`;

export const USER_VERIFY_URL = `${USER_URL}/verify`;

export const USER_STATE_URL = `${USER_URL}/state`;
